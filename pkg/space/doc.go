// Package space defines the authoritative space configuration schema and the
// Space-service-owned filesystem layout used to persist config and opaque
// records supplied by higher-level domain owners.
package space
